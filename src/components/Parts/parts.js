import React, { Fragment } from "react";
import AvailableParts from "./AvailableParts";


const Parts = (props) => {
  
  return (
    <Fragment>
      <AvailableParts/>
    </Fragment>
  );
};

export default Parts;
